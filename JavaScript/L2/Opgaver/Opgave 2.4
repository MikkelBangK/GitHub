let string = "Nikolaj bliver spist af en meget farlig raptor som hedder Benjamin"

let liste = string.split(' ')
let map = {};
for (let e of liste){
    if (e in map){
        map[e]++;
    } else {
        map[e] = 1;
    }
}
console.log(liste);
console.log(map)