// bubbleSort.js
let list = [1, 2, 5, 3, 2, 19, 99999, 6969, 420, 42069, 69420, 1, 4, 5, 99, 2277] 

function bubbleSort(array){
for (let i = list.length - 1; i >= 0; i--) {
    for (let j = 0; j <= i - 1; j++) {
        if (list[j] > list[j + 1]) {
            let temp = list[j];
            list[j] = list[j+1];
            list[j+1] = temp;
        }
    }
}
}
console.log(list.toString()); // => 0,1,2,4,7,8,9,13,16

function binarySearch(array, element){

let left = 0;
let right = list.length - 1;
let found = false;
let target = 420;

while(found == false && left <= right){
    let m = Math.floor((left + right) / 2);
    if(list[m] == target) {
        found = true;
        console.log(target + " Index fundet på plads " + list.indexOf(target));
    } else if (list[m] < target) {
        left = m + 1;
    } else if(list[m] > target) {
        right = m - 1;
    } else {
        console.log(-1);
        found = true;
    }
}
}